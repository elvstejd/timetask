import styled from 'styled-components';

export const CardTitle = styled.div`
    color: var(--text-primary);
    margin-bottom: .5rem;
    display: flex;
    align-items: center;

    h3 {
        margin-left: 0.2rem;
    }
`;
