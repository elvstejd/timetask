import styled from 'styled-components';

export const CardTitle = styled.div`
    color: var(--text-secondary);
    margin-bottom: .5rem;
    display: flex;
    align-items: center;

    h3 {
        margin-left: 0.3rem;
        font-weight: 500;
        font-size: var(--md);
    }
`;
